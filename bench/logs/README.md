# Captured run output

Console output from the benchmark runs of 2026-08-07, transcribed from the terminal
session that produced them. These are the observed results, not auto-captured log files:
the harness printed to stdout and was not writing to disk at the time.

`l1-evidence.json` is different. It was generated by querying Solana devnet directly and
can be regenerated at any time.

The ER-side runs cannot be reproduced, because the M0 program they ran against has been
closed. See ../RESULTS.md.
